-- El generador de carruseles con IA se eliminó por decisión de producto
-- (reemplazado por un recurso educativo estático en /dashboard/carousels).
-- Esto borra la tabla, sus datos y sus políticas RLS de forma permanente.
-- ⚠️ Acción irreversible: confirmado explícitamente por el usuario antes de aplicar.
DROP TABLE IF EXISTS public.carousels;
